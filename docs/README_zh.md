# 整理说明

以用户提供的 MOR 投稿稿为依据，主目录 README 包含图 1–7、附录 J.1 与程序的对应表。

- `python/` 和 `r/`：当前论文主要绘图代码。
- `supplementary/`：相关探索程序，不作为当前论文图表的确定复现入口。
- `archive/original_github/`：本次更新前 GitHub 文件的原样备份。
- `reference_figures/`：本地已有参考图，未宣称与投稿稿逐图一致。
- `docs/source_manifest.json`：原始相对路径、原文件哈希和修改记录。

本地原始目录和已有 zip 不改动。压缩包不包含论文 PDF、WhatsApp 数据、网络推断项目、
R 会话缓存和重复目录。运行步骤见根目录 README。

已知缺口：未找到当前图 4 的独立绘图源码；附录 J.1 的旧版随机响应振幅与当前正文不同，
需要作者核对。完整大规模仿真是否执行，以及检查结果，以 VALIDATION.md 为准。

本次修复附录脚本中未定义的 ln_values_new，并用原有目标函数补齐图 3 的三组 p 曲线；原单组 p 脚本另行保留，未修改模型公式。
